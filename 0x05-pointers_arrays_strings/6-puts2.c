#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * puts2 - print upper case letters.
 *
 * @str: char
 * Return: always 0
 */
void puts2(char *str)
{
	int i;

	for (i = 0; i < l; i = i + 2)
	{
		puts(str(i));
	}
}
